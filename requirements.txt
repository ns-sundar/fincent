# --- LLM + orchestration ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.40
langgraph-checkpoint-sqlite>=2.0.0
langserve[server]>=0.3.0

# --- API + serving ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=1.3.0,<2.0.0
openai>=1.0.0
markdown-it-py>=3.0.0

# --- UI (separate process) ---
streamlit>=1.38.0
requests>=2.32.0
# Plotly powers the Portfolio tab charts/tables (balances pie, tables).
plotly>=5.24.0
# Pandas gives us DataFrames for the portfolio graphics in Streamlit.
pandas>=2.2.0
# NumPy powers vectorized Goal Planning Monte Carlo simulations.
numpy>=1.26.0

# --- RAG (Q&A agent) ---
# FAISS CPU-only vector store -- persisted at /data/vector_db.
faiss-cpu>=1.8.0
# Document parsing: BeautifulSoup for HTML, PyPDF for PDF.
beautifulsoup4>=4.12.0
lxml>=5.0.0
pypdf>=4.0.0
# Token-aware recursive chunking (~1000 tokens, 200 overlap).
tiktoken>=0.7.0
# Model Context Protocol server that exposes the FAISS vector_db as a tool
# (src/rag/mcp_server.py). Optional at runtime: only imported when the MCP
# sidecar is launched; the in-process Q&A agent does not require it.
mcp>=1.2.0

# --- Portfolio agent tools (MCP) ---
# LangChain adapters that convert MCP tool definitions into LangChain
# ``BaseTool`` objects. Used by the Portfolio agent to call the OpenBB
# MCP server and the Fincent RAG MCP server as tools.
langchain-mcp-adapters>=0.1.9,<0.2
# OpenBB Platform MCP server (real-world financial data). Launched as
# a subprocess in stdio mode by the Portfolio agent's MCP client.
# ``openbb`` is the Platform meta-package; it pulls in yfinance (free,
# no key) and other default providers. Additional providers such as
# FinancialModelingPrep can be installed via their own extras and
# configured with API keys in ``~/.openbb_platform/user_settings.json``.
openbb-mcp-server>=1.4.0
openbb>=4.3.0
# Financial Modeling Prep MCP server. Uses the same FMP key aliases resolved
# by src/agents/market_research/mcp_tools.py.
fmp-mcp>=0.3.3

# --- Config / data ---
pydantic>=2.7.0
pydantic-settings>=2.4.0
PyYAML>=6.0.2
python-dotenv>=1.0.1
jsonschema>=4.23.0

# --- Tests ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
