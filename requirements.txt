# --- LLM + orchestration ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.40
langgraph-checkpoint-sqlite>=2.0.0
langserve[server]>=0.3.0

# --- API + serving ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=1.3.0,<2.0.0

# --- UI (separate process) ---
streamlit>=1.38.0
requests>=2.32.0

# --- RAG (Q&A agent) ---
# FAISS CPU-only vector store -- persisted at /data/vector_db.
faiss-cpu>=1.8.0
# Document parsing: BeautifulSoup for HTML, unstructured for PDF.
beautifulsoup4>=4.12.0
lxml>=5.0.0
unstructured[pdf]>=0.15.0
# Fallback PDF parser when unstructured cannot handle a file.
pypdf>=4.0.0
# Token-aware recursive chunking (~1000 tokens, 200 overlap).
tiktoken>=0.7.0
# Model Context Protocol server that exposes the FAISS vector_db as a tool
# (src/rag/mcp_server.py). Optional at runtime: only imported when the MCP
# sidecar is launched; the in-process Q&A agent does not require it.
mcp>=1.2.0

# --- Config / data ---
pydantic>=2.7.0
pydantic-settings>=2.4.0
PyYAML>=6.0.2
python-dotenv>=1.0.1

# --- Tests ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
